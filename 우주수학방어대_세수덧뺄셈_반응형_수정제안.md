# 우주 수학 방어대 — "세 수의 덧셈과 뺄셈" 전환 + 반응형 최적화 제안서

> 작성일: 2026-09-17 / 상태: **검토 대기**

---

## 1. 문제 유형 변경 — 두 수 → 세 수

현재는 **두 수**의 덧셈/뺄셈(`5 + 3 = ?`)입니다. 초등 1학년 2학기 교육과정의 **"세 수의 덧셈과 뺄셈"** 단원에 맞춰
숫자 3개, 연산 2번짜리 문제(`2 + 3 + 4 = ?`)로 바꿉니다.

### 1-1. 난이도 3단계 재설계
| 단계 | 지금 | 바뀜 |
|---|---|---|
| 1단계 | 10 이하 두 수 덧셈 | **세 수의 덧셈** — 한 자리 수 3개, 받아올림 없이 (예: `2+3+4=9`, 합 9 이하) |
| 2단계 | 10 이하 두 수 뺄셈 | **세 수의 뺄셈** — 한 자리 수 범위, 받아내림 없이 (예: `9-3-2=4`, 중간 결과도 항상 0 이상) |
| 3단계 | 20 이하 혼합 | **세 수의 덧셈과 뺄셈 혼합** — 덧셈·뺄셈이 섞임 (예: `4+3-2=5`, `9-4+3=8`), 20 이하 |

- 계산은 **앞에서부터 순서대로**(`(a+b)+c` 또는 `(a-b)-c`처럼 학교에서 배우는 방식) 진행 — 중간값이 음수가 되거나 10을 넘어 받아올림/받아내림이 생기지 않도록 생성 시 걸러냄 (1·2단계 한정, 3단계는 20 이하까지 허용)
- 정답 선택지(오답 몬스터) 생성 로직은 지금처럼 정답 근처 숫자로 자동 생성 — 그대로 재사용

### 1-2. 화면 표시
- 문제 텍스트를 `2 + 3 + 4 = ?` 형태로 표시 (숫자 3개 + 연산기호 2개라 기존보다 길어짐 → 폰트 크기를 화면 너비에 맞춰 자동 축소)
- 보스전 문제도 동일하게 세 수로 전환

### 1-3. 기존 기능과의 연결
- 웨이브/보스/기체/업그레이드/2인용 시스템은 **문제 생성 함수만 교체**하면 되므로 그대로 유지됩니다.

---

## 2. 반응형 웹 최적화

지금은 데스크톱 기준으로는 잘 보이지만, 실제 배포 링크를 **휴대폰/태블릿**에서 열었을 때 아래를 점검·수정합니다.

### 2-1. 점검할 화면 크기
- 모바일 세로 (375×667, 390×844 등 — 아이폰 표준)
- 모바일 가로 (667×375) — **이 게임은 가로 슈팅게임이라 가로모드가 훨씬 잘 맞음**
- 태블릿 (768×1024)
- 좁은 데스크톱 창 (900×600 등)

### 2-2. 구체적으로 고칠 부분
| 문제 | 해결 |
|---|---|
| 세로로 긴 휴대폰 화면에서 캔버스가 좁아 몬스터 3마리가 다닥다닥 붙음 | 화면이 **세로로 길고 좁을 때** "📱 화면을 가로로 돌려주세요" 안내 오버레이 표시 (가로모드 권장) |
| 문제 텍스트(`2+3+4=?`)가 길어져서 작은 화면에서 HUD 박스를 벗어날 수 있음 | `clamp()`로 화면 너비에 비례해 폰트 크기 자동 조절 |
| HUD 상단바(하트·점수·P2점수·웨이브도트·버튼)가 좁은 화면에서 겹치거나 줄바꿈됨 | 좁은 화면에서는 일부 정보(웨이브 도트 등)를 작게 줄이거나 두 줄로 재배치 |
| 격납고 기체 카드가 2열 고정이라 아주 작은 화면(320px대)에서 카드가 짜부러짐 | 화면 너비에 따라 1~3열로 자동 조정 |
| 노치가 있는 아이폰에서 하단 터치 버튼이 화면 끝에 가려질 수 있음 | `safe-area-inset` 여백 적용 |
| 화면 회전 시(세로↔가로) 캔버스 크기가 즉시 안 바뀔 수 있음 | `orientationchange` 이벤트도 감지해서 즉시 재계산 |
| 태블릿/큰 화면에서 캔버스가 과도하게 늘어나 우주선·몬스터가 작아 보임 | 콘텐츠 최대 폭 제한(예 1100px) 후 좌우 여백 처리 |

### 2-3. 터치 조작 재확인
- 1인용: 좌우 드래그 + 발사 버튼 (기존 유지)
- 2인용: 화면 좌/우 반분 + 구분선 (기존 유지, 좁은 화면에서도 절반씩은 최소 조작 가능한 너비 확보 확인)

---

## 3. 작업 범위 확인

1. 문제 생성 로직을 "세 수" 버전으로 교체 (1·2·3단계 모두)
2. HUD 문제 표시 폰트 반응형 처리
3. 화면 크기별 레이아웃 점검 및 수정 (위 표 항목들)
4. 모바일 세로 모드 회전 안내 오버레이 추가
5. 실제 여러 해상도로 스크린샷 검증 후 GitHub push → Vercel 자동 재배포

바로 진행해도 될까요, 아니면 단계별 숫자 범위(예: 1단계 합계 상한을 9로 할지 다르게 할지)를 더 조정하고 싶으신 부분이 있을까요?
